[tool.poetry]
name = "customer-churn-pipeline"
version = "0.1.0"
description = "Customer churn prediction pipeline"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"
packages = [{include = "customer_churn_data_pipeline"}]

[tool.poetry.dependencies]
python = ">=3.9,<3.12" # Modified Python version constraint
apache-airflow = "^2.7.1" # or whatever version you need
pandas = "^2.1.0"
scikit-learn = "^1.3.0"
# ... other dependencies ...

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"