[project]
name = "customer-churn-data-pipeline"
version = "0.1.0"
description = ""
authors = [
    {name = "nil186007",email = "nil.cst@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "customer_churn_data_pipeline", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
